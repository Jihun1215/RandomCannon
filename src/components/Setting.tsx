import styled from "styled-components"

export const Setting = () => {
  return (
    <Modal>Setting</Modal>
  )
}

const Modal = styled.div`
    width: 50%;
    height: 100%;
    border: 1px solid red;
`;